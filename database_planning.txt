Database Tables

Messages Table
primary_key,
author_key(matches primary_key in user), connects to the Author table
timeSent,
content,
image_key(matches primary_key in user), connects to the Avatar table


User Table
primary_key,
name
image
